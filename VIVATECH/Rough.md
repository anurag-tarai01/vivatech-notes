Read and ask question to last antigravity talk:

domain_currency we can actually fetch them from active domains foreign and local currency  
  
clearly explain these three  
@Column(name = "effective_rate", precision = 19, scale = 8) private BigDecimal effectiveRate; @Column(name = "spread_rate", precision = 10, scale = 6) private BigDecimal spreadRate; @Column(name = "inverse_rate", precision = 19, scale = 8) private BigDecimal inverseRate;