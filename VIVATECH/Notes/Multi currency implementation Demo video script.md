Estimated time multi domain: Even we give a timelime, this is not implementation. This is implementation with migration of legacy code. During develpoment, are ye bhi karna tha, which is not added into time line. 

System support multi currency, during setup we have it add a default domain with foreign currency USD and local currency Somalia Shiling(SOS). 
As first step we have to setup all the core wallets including FX commission wallet, we have curls for those setup

Let generate float, USD and SOS



